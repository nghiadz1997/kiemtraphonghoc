import { EventItem, TaskItem } from "@/types";

export function generateDemoEvents(_userId: string): EventItem[] {
  return [];
}

export function generateDemoTasks(_userId: string): TaskItem[] {
  return [];
}
